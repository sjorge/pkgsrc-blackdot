####
## Blackdot pkgsrc repository
###########################################
Replacement for the old omnios.blackdot.be repository.

The pkgsrc repository provided by joyent provides most 
packages that were available in the older omnios repository.

This repository is a suppliment to the joyent repository,
packages are prefixed with pbd. It provides the few missing bits.

Everything is provided as-is. Some packages are taken from pkgsrc-wip.

####
## Repository Information
###########################################
Compatibility:	SmartOS
Architecture:	x86_64
Target:		LTS
Siging:		no (you may be prompted to accept unsigned packages)
Stability:	Highly Experimental
URL:		http://pkg.blackdot.be/${PKG_VER}/${PKG_ARCH}/All

####
## Adding the repository
###########################################
PKG_VER=$(cat /opt/local/etc/pkgin/repositories.conf  | grep 'SmartOS' | cut -d"/" -f 6)
PKG_ARCH=$(cat /opt/local/etc/pkgin/repositories.conf  | grep 'SmartOS' | cut -d"/" -f 7)
echo "http://pkg.blackdot.be/${PKG_VER}/${PKG_ARCH}/All" >> /opt/local/etc/pkgin/repositories.conf

####
## TODO
## ----------------------------------------
## - low priority
## = normal priority
## + high priority
## ! critical;
## > in progress
###########################################
[+] Provide packages
    [+] couchpotato
    [-] Sonnarr 
[=] Provide SMF manifests
    [=] znc
    [=] sabnzbd
    [=] sickbeard
    [-] couchpotato
[-] Testing and optimization
[-] OmniOS compatibility
